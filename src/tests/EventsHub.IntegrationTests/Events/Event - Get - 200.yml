info:
  name: Event - Get - 200
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/events/:eventid"
  params:
    - name: eventid
      value: 115336f2-7515-4101-b011-64b9a78de1e9
      type: path
  auth: inherit

runtime:
  scripts:
    - type: tests
      code: |-
        test ("Status code is 200 OK", function() {
          expect(res.getStatus()).to.equal(200);
        })

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
